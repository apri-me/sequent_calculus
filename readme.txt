In this project library openpyxl has been used and this library is not included in python standard library.
Try to install it via:

> pip install openpyxl

Example of a right well-formed formula:
( ( p \/ q ) -> ( ~ ( (~ p) /\ (~ q) ) ) )